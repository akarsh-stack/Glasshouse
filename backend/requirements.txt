fastapi>=0.111
uvicorn[standard]>=0.29
# Upper bound deliberate: 0.0.18 was an open-ended floor, so CI resolved a
# newer sqlmodel than local and its naive-datetime validation turned every
# trace write into an error there while passing here. Pin the range so the
# two environments agree.
sqlmodel>=0.0.39,<0.1
anthropic>=0.28
chromadb>=0.5
redis>=5.0
cachetools>=5.3
pypdf>=4.0
python-docx>=1.1
python-multipart>=0.0.9
pydantic-settings>=2.2
httpx>=0.27
websockets>=12.0

# Embeddings run through chromadb's bundled ONNX MiniLM by default
# (EMBEDDING_PROVIDER=onnx) — same all-MiniLM-L6-v2 model, 384 dims, no PyTorch.
# To use the PyTorch backend instead, install requirements-torch.txt and set
# EMBEDDING_PROVIDER=sentence-transformers. See docs/adr/005-onnx-embeddings.md.
